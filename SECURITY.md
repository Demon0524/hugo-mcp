# Security Policy

## 报告安全问题

请不要在公开 Issue、Pull Request 或讨论区发布可利用的漏洞细节、Token、真实站点路径或生产数据。

优先通过 GitHub 私人漏洞报告功能提交。如果该功能不可用，请联系仓库维护者 Demon0524，并提供：

- 受影响的版本或提交；
- 复现步骤和最小化示例；
- 影响范围，例如未授权访问、路径越界、任意文件写入或凭据泄露；
- 建议的修复方式（如有）。

收到报告后，维护者会先确认问题，再评估影响、修复和公开时间。请给维护者合理的修复窗口，不要在修复前公开漏洞。

## 安全边界

- 生产博客内容、Token、备份和审计日志不属于公开仓库。
- docker03 不自动部署 `main` 或任意 Pull Request。
- 公开贡献必须经过 CI 和人工审核。
- MCP 服务的写操作依赖 Bearer Token、路径校验、乐观锁、审计和软删除；改变这些边界的 PR 需要补充安全测试。
